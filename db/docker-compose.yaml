version: '3.8'

services:
  postgres-db:
    image: postgres:17
    environment:
      POSTGRES_USER: "bob"
      POSTGRES_PASSWORD: "bobpass"
      POSTGRES_DB: "bobdb"

    volumes:
      - "postgres-data:/var/lib/postgresql/data"

    ports:
      - "5432:5432"

volumes:
  postgres-data:
